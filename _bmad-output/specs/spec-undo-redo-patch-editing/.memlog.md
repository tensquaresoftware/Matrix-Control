---
topic: Editorial Undo/Redo for patch and master editing
slug: undo-redo-patch-editing
mode: express
updated: 2026-08-23T23:58
---

- (note) Brownfield verified: APVTS built with nullptr UndoManager in PluginProcessor.cpp; no UndoManager elsewhere in repo
- (note) Existing pattern: setProperty(..., nullptr) for non-editorial UI state (clipboard, mutator, feedback)
- (note) Parameter flow: valueTreePropertyChanged → apvtsToBuffer → SysEx dispatch with suppress* flags for bulk ops
- (note) beginChangeGesture/endChangeGesture used for Track Generator, envelopes, SliderAttachment on ParameterCell
- (note) Patch Mutator: MutationHistoryStore separate from editorial undo; Compare via kCompareActive property
- (note) DirtyPatchTracker: 134-byte buffer snapshot vs current; syncApvtsAndIsDirty for APVTS-driven edits
- (note) User manual §7 states no Undo/Redo; patch name display read-only in current manual
- (capability) CAP-1 intent: User can undo the last editorial transaction restoring prior patch/master/matrix-mod parameter state
- (capability) CAP-1 success: One Cmd/Ctrl+Z after a grouped gesture restores pre-gesture values and synth hears restored state via existing MIDI path
- (capability) CAP-2 intent: User can redo after undo to restore undone editorial state
- (capability) CAP-2 success: Cmd/Ctrl+Shift+Z reapplies undone transaction; synth and UI match restored state
- (capability) CAP-3 intent: Standard undo/redo keyboard shortcuts work in Standalone and hosted plugin when focus rules allow
- (capability) CAP-3 success: macOS Cmd+Z/Cmd+Shift+Z and Windows/Linux Ctrl+Z/Ctrl+Shift+Z trigger undo/redo per keyboard-shortcuts.md
- (capability) CAP-4 intent: Related parameter changes from one user gesture form a single undo transaction
- (capability) CAP-4 success: Slider drag, envelope drag, Track Generator drag, Init module, Paste module, Matrix Mod bus reorder each produce one undo step
- (capability) CAP-5 intent: After undo or redo the synthesizer reflects restored APVTS state through existing model→SysEx pipeline
- (capability) CAP-5 success: Post undo/redo SysEx matches restored parameters without breaking suppress flags or Matrix Mod coalesce behavior
- (capability) CAP-6 intent: Dirty patch indicator reflects true divergence from load snapshot after undo/redo
- (capability) CAP-6 success: DirtyPatchTracker isDirty/syncApvtsAndIsDirty matches user expectation after edit→undo→clean and edit→undo→redo cycles
- (constraint) Core must not depend on GUI; undo logic in Core/processor; shortcuts at editor or appropriate GUI layer
- (constraint) Do not record undo for navigation, clipboard action properties, mutator history selection, Compare active, MIDI ports, skin, UI scale, latency, device detection
- (constraint) Patch Mutator MUTATE/RETRY history remains separate; editorial undo must not replace mutator navigation
- (constraint) Mutator Compare (C) is A/B audition; must not collide with Cmd+Z semantics
- (constraint) Patch load, file load, internal patch navigation: editorial undo stack clears or resets baseline (see undo-policy.md)
- (constraint) STORE SAVE SAVE AS bank transfer import/export: persistent effects non-undoable in v1
- (constraint) English-only source code; UI strings English; JUCE 9.0.1; no regression on suppress flags and Matrix Mod SysEx coalesce
- (constraint) Update Documentation/User/manuel-utilisateur.md to remove no-undo statement and document shortcuts
- (constraint) Undo stack depth: transaction count not raw parameters; default ~100 transactions with oldest eviction
- (constraint) v1 keyboard shortcuts only; no visible Undo/Redo menu buttons unless spec explicitly adds them later
- (constraint) No host DAW automation undo integration in v1; plugin-internal undo only
- (decision) Non-goals: undo patch/bank navigation, settings prefs, STORE/SAVE operations, replace mutator history, mutator Compare mode, host undo, configurable depth UI, visible undo buttons v1
- (assumption) Patch name inline rename is not in current product (manual read-only); CAP scope assumes parameter edits only until rename ships
- (question) MUTATE/RETRY: single undo transaction vs full stack clear? Recommendation: clear stack and set new baseline like patch load — avoids hundreds of entries and matches mutate-as-new-branch mental model
- (question) Undo while Mutator Compare active: disabled vs force exit Compare then undo? Recommendation: disable undo during Compare; user exits Compare via C first
- (question) Patch name inline rename in undo v1? Recommendation: defer — name read-only today; include when inline edit ships
- (question) Master Edit parameters in v1 undo scope? Recommendation: include — same APVTS/master SysEx path; excluding master would confuse users on Master panel
- (question) Patch load: clear stack only vs also offer undo-to-loaded checkpoint? Recommendation: clear stack on load; loaded state is implicit baseline; no undo-to-loaded without re-load
- (question) Slider arrow keys repeated: one transaction vs per key? Recommendation: one transaction per focus session until focus lost (align with gesture grouping)
- (question) Exact default transaction limit for v1? Recommendation: 100 transactions per Guillaume direction; document memory tradeoff in undo-policy
- (question) Full internal patch paste (pasteFullPatch): same rule as patch load? Recommendation: yes — clear stack; paste is a new working baseline
- (direction) Intercept shortcuts at PluginEditor when no active text field; do not steal Cmd+Z from PatchNameDisplay Settings dialogs
- (note) Companions: undo-policy.md keyboard-shortcuts.md state-machines.md; adopted CONVENTIONS.md and project-context.md
- (event) Self-validate pass 1 coherence: Spec Law 1-6 and 8 satisfied after open questions logged not invented
- (event) Self-validate pass 2 preservation: all load-bearing input claims routed to SPEC or companions; wrapper ceremony dropped
- (decision) Story 12-1: Spike UndoManager wired to APVTS + one slider + MIDI proof
- (decision) Story 12-2: Bulk transactions Init/Paste module with suppress hooks
- (decision) Story 12-3: Interactive displays envelope and Track Generator gesture transactions
- (decision) Story 12-4: Matrix Mod reorder keyboard shortcuts editor wiring user manual
- (decision) Story 12-5: Mutator load policy dirty tracker tests Core
- (event) stories.yaml schema check: 5 stories ids unique prefix-free required fields present
- (decision) Open Q1 resolved: MUTATE/RETRY clear editorial undo stack; new baseline like patch load
- (decision) Open Q2 resolved: undo/redo disabled while Mutator Compare active
- (decision) Open Q3 resolved: patch name inline rename out of undo v1 scope until inline edit ships
- (decision) Open Q4 resolved: Master Edit parameters included in v1 editorial undo scope
- (decision) Open Q5 resolved: patch load clears undo stack; loaded state is implicit baseline; no undo-to-loaded without re-load
- (decision) Open Q6 resolved: NOT one arrow key per undo; mouse slider drag (sliding) is one undo transaction; arrow keys grouped per focus session until focus lost — undo restores pre-session value redo restores post-session value
- (decision) Open Q7 resolved: default 100 editorial undo transactions with oldest eviction
- (decision) Open Q8 resolved: pasteFullPatch clears stack same as patch load
- (event) Open questions closed by user confirmation 2026-08-23; SPEC re-derived
